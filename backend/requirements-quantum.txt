# OPTIONAL quantum demo dependencies (not installed by default).
#
# These power the /recommend/quantum endpoint. They are heavy and only needed
# if you want to try the QAOA diversity re-ranking demo.
#
# To enable:
#   1. pip install -r backend/requirements-quantum.txt
#   2. set ENABLE_QUANTUM=true
#
qiskit==1.3.1
qiskit-algorithms==0.3.1
qiskit-optimization==0.6.1
