# Copy to ".env" to override defaults. Everything works without this file.
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=recipe_db

# Set to "true" and install backend/requirements-quantum.txt to enable the
# optional QAOA diversity demo at POST /recommend/quantum.
ENABLE_QUANTUM=false
